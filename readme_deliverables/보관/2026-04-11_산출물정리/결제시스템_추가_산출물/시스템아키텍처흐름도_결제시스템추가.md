# ReadMe 쇼핑몰 — 시스템 아키텍처 흐름도 (결제 시스템 추가)

## 📄 문서 개요

| 항목 | 내용 |
|---|---|
| 프로젝트명 | 교보문고 쇼핑몰 시스템 (README) |
| 개발 환경 | Java 21 / Spring Boot 3.5 / PostgreSQL / Vue.js 3 |
| 작성일 | 2026.03.28 |
| 버전 | v1.2 |
| 기준 문서 | 시스템_아키텍처_흐름도.md |
| 변경 범위 | PaymentService 외부 PG 연동 구조 추가 / 결제 흐름도 PG별 분기 반영 |

---

## 1. 전체 시스템 계층 흐름도 (결제 PG 연동 추가)

> 기존 흐름도에서 PaymentService 하단에 PG Gateway 레이어가 추가됩니다.

```mermaid
flowchart TD
    subgraph CLIENT["🖥️ 프론트엔드 (Vue.js 3)"]
        VUE["Vue.js 클라이언트 (SPA)"]
        TOSS_SDK["@tosspayments/payment-widget\n(토스 SDK — 클라이언트 전용)"]
        NAVER_SDK["Naver Pay JS SDK\n(네이버 SDK — 클라이언트 전용)"]
    end

    subgraph GATEWAY["🔐 Spring Security / JWT 필터"]
        JWT["JwtAuthFilter\n토큰 검증 / 사용자 인증"]
    end

    subgraph CONTROLLER["📡 Controller Layer (REST API)"]
        PAC["MyPaymentApiController\nPOST /order/payment/ready\nPOST /order/payment/confirm\nPOST /order/payment/approve\nPOST /order/payment/fail"]
        ADPAC["AdminPaymentApiController\n/admin/payment"]
        OC["MyOrderApiController\nPATCH /mypage/order/{id}/cancel"]
    end

    subgraph SERVICE["⚙️ Service Layer"]
        PAS["PaymentService\n- 금액 위변조 검증\n- PG Gateway 라우팅\n- payment 상태 관리"]
        OS["OrderService\n- 주문 상태 관리"]
    end

    subgraph PG_GATEWAY["🌐 PG Gateway Layer (외부 API 추상화)"]
        direction LR
        PGI["PaymentGateway\n<<interface>>\n+ ready()\n+ confirm()\n+ cancel()"]
        TOSS_GW["TossPaymentGateway\n(provider=TOSS)"]
        KAKAO_GW["KakaoPaymentGateway\n(provider=KAKAO)"]
        NAVER_GW["NaverPaymentGateway\n(provider=NAVER)"]
        PGI --> TOSS_GW
        PGI --> KAKAO_GW
        PGI --> NAVER_GW
    end

    subgraph EXTERNAL["☁️ 외부 PG API"]
        TOSS_API["토스페이먼츠 API\napi.tosspayments.com\n- POST /v1/payments/confirm\n- POST /v1/payments/{key}/cancel"]
        KAKAO_API["카카오페이 API\nkapi.kakao.com\n- POST /v1/payment/ready\n- POST /v1/payment/approve\n- POST /v1/payment/cancel"]
        NAVER_API["네이버페이 API\ndev.pay.naver.com\n- 결제 준비/승인/취소"]
    end

    subgraph REPO["🗄️ Repository Layer"]
        PAR["PaymentRepository"]
        OR["OrderRepository"]
        DR["DeliveryRepository"]
    end

    subgraph DB["🐘 PostgreSQL"]
        T6[("payment\n+provider\n+payment_key\n+installment_months\n+failure_reason")]
        T5[("order")]
        T7[("delivery")]
    end

    VUE -->|"HTTP Request (JWT)"| JWT
    TOSS_SDK -->|"successUrl / failUrl 리다이렉트"| VUE
    NAVER_SDK -->|"returnUrl 리다이렉트"| VUE
    JWT --> PAC
    JWT --> OC

    PAC --> PAS
    OC --> PAS
    ADPAC --> PAS
    PAS --> OS

    PAS -->|"provider 기반 라우팅"| PGI
    TOSS_GW -->|"REST API 호출"| TOSS_API
    KAKAO_GW -->|"REST API 호출"| KAKAO_API
    NAVER_GW -->|"REST API 호출"| NAVER_API

    PAS --> PAR
    PAS --> OR
    PAS --> DR

    PAR --> T6
    OR --> T5
    DR --> T7
```

---

## 2. 도메인별 요청 처리 흐름

### 2-1. 회원 인증 흐름 (회원가입 / 로그인 / 로그아웃)

```mermaid
flowchart TD
    A([클라이언트 요청]) --> B{요청 유형}

    B -->|POST /signup| C[AuthController.signup]
    C --> D[MemberService.signup]
    D --> E{이메일 중복 확인\nMemberRepository.existsByEmail}
    E -->|중복| F[409 Conflict 반환]
    E -->|사용 가능| G[BCrypt 비밀번호 암호화]
    G --> H[MemberRepository.save]
    H --> I[(member 테이블 INSERT)]
    I --> J[201 Created 반환]

    B -->|POST /signin| K[AuthController.signin]
    K --> L[MemberService.signin]
    L --> M[MemberRepository.findByEmail]
    M --> N{회원 존재 여부}
    N -->|없음| O[401 Unauthorized 반환]
    N -->|있음| P{비밀번호 검증\nBCrypt.matches}
    P -->|불일치| O
    P -->|일치| Q{MemberStatus 확인}
    Q -->|DELETE / DEACTIVATE| R[403 Forbidden 반환]
    Q -->|ACTIVATE| S[JwtUtil.generateAccessToken]
    S --> T[JwtUtil.generateRefreshToken]
    T --> U[200 OK + AccessToken + RefreshToken 반환]

    B -->|POST /signout| V[AuthController.signout]
    V --> W[MemberService.signout]
    W --> X[RefreshToken 무효화]
    X --> Y[200 OK 반환]
```

---

### 2-2. 상품 조회 흐름 (회원 / 관리자)

```mermaid
flowchart TD
    A([클라이언트 요청]) --> B{요청 주체}

    B -->|일반 회원| C{요청 유형}
    C -->|GET /products| D[ProductController.getProductList]
    C -->|GET /products/{id}| E[ProductController.getProductDetail]
    C -->|GET /products/search| F[ProductController.searchProducts]

    D --> G[ProductService.getProductList\nPageable + keyword]
    E --> H[ProductService.getProductDetail\n+ increaseViewCount]
    F --> I[ProductService.searchProducts]

    G --> J[ProductRepository.findAllByStatusAndKeyword]
    H --> K[ProductRepository.findByIdAndStatus]
    H --> L[ProductImageRepository.findByProductId]
    I --> J

    J --> M[(product 테이블 SELECT)]
    K --> M
    L --> N[(product_image 테이블 SELECT)]
    M --> O[ProductResponse 반환]
    N --> O
    O --> P[200 OK + 상품 데이터]

    B -->|관리자| Q{관리자 요청 유형}
    Q -->|GET /admin/product| R[AdminProductController.getProductList]
    Q -->|POST /admin/product| S[AdminProductController.createProduct]
    Q -->|PUT /admin/product/{id}| T[AdminProductController.updateProduct]
    Q -->|DELETE /admin/product/{id}| U[AdminProductController.deleteProduct]

    R --> G
    S --> V[ProductService.createProduct]
    T --> W[ProductService.updateProduct]
    U --> X[ProductService.deleteProduct\nsoft delete: deletedAt 설정]

    V --> Y[(product 테이블 INSERT)]
    W --> Z[(product 테이블 UPDATE)]
    X --> AA[(product.deleted_at = NOW UPDATE)]
```

---

### 2-3. 장바구니 흐름

```mermaid
flowchart TD
    A([인증된 회원 요청]) --> B{장바구니 요청 유형}

    B -->|GET /cart| C[CartController.getCart]
    B -->|POST /cart/item| D[CartController.addItem]
    B -->|PATCH /cart/item/{id}| E[CartController.updateQuantity]
    B -->|DELETE /cart/item/{id}| F[CartController.removeItem]

    C --> G[CartService.getCart\nLong memberId]
    D --> H[CartService.addItem\nCartItemRequest + memberId]
    E --> I[CartService.updateQuantity\nCartUpdateRequest + memberId]
    F --> J[CartService.removeItem\nitemId + memberId]

    G --> K[CartRepository.findByMemberId]
    G --> L[CartItemRepository.findByCartId]
    K --> M[(cart 테이블 SELECT)]
    L --> N[(cart_item 테이블 SELECT)]
    M --> O[CartResponse 반환]
    N --> O

    H --> P{상품 유효성 확인\nProductRepository.findByIdAndStatus}
    P -->|상품 없음/비활성| Q[404 Not Found]
    P -->|유효| R{중복 상품 확인}
    R -->|이미 담긴 상품| S[수량 증가 UPDATE]
    R -->|신규| T[CartItemRepository.save]
    S --> U[(cart_item 테이블 UPDATE)]
    T --> V[(cart_item 테이블 INSERT)]

    I --> W[CartItemRepository.save\n수량 업데이트]
    W --> X[(cart_item 테이블 UPDATE)]

    J --> Y[CartItemRepository.deleteAllByIdIn]
    Y --> Z[(cart_item 테이블 DELETE)]
```

---

### 2-4. 주문 생성 ~ 결제 흐름 (Mock PG 기준)

```mermaid
flowchart TD
    A([인증된 회원]) --> B[OrderController.createOrder\nPOST /mypage/order]
    B --> C[OrderService.createOrder\nOrderRequest + memberId]

    C --> D{장바구니 상품 검증\nProductRepository.findByIdAndStatus}
    D -->|재고 부족| E[409 재고 부족 반환]
    D -->|유효| F[Order 엔티티 생성\nOrderStatus=PENDING]
    F --> G[OrderItem 생성\n상품 스냅샷 저장]
    G --> H[OrderRepository.save]
    G --> I[OrderItemRepository.saveAll]
    H --> J[(order 테이블 INSERT)]
    I --> K[(order_item 테이블 INSERT)]
    J --> L[OrderResponse 반환]

    L --> M[PaymentController.requestPayment\nPOST /payment/request]
    M --> N[PaymentService.requestPayment]
    N --> O[Payment 엔티티 생성\nPaymentStatus=READY]
    O --> P[PaymentRepository.save]
    P --> Q[(payment 테이블 INSERT)]
    Q --> R[PG사 결제 요청\n외부 API 호출]

    R --> S{결제 결과}
    S -->|성공 콜백| T[PaymentController.completePayment\nPOST /payment/complete]
    T --> U[PaymentService.completePayment]
    U --> V[PaymentStatus = PAID 업데이트]
    U --> W[OrderStatus = PAYED 업데이트]
    U --> X[Delivery 엔티티 생성\nDeliveryStatus=READY]
    V --> Y[(payment 테이블 UPDATE)]
    W --> Z[(order 테이블 UPDATE)]
    X --> AA[DeliveryRepository.save]
    AA --> AB[(delivery 테이블 INSERT)]
    AB --> AC[200 OK 결제 완료]

    S -->|실패| AD[PaymentStatus = FAILED 업데이트]
    AD --> AE[OrderStatus = CANCELED 업데이트]
    AE --> AF[400 결제 실패 반환]
```

---

### 2-4-T. 토스페이먼츠 결제 흐름

```mermaid
flowchart TD
    A([인증된 회원]) --> B["결제 수단 선택: 토스페이먼츠\n(Vue.js 결제 페이지)"]

    B --> C["클라이언트:\ntossPayments.requestPayment()\n(서버 API 호출 없음)"]
    C --> D["토스 결제창 렌더링\n(카드번호 입력 / 간편결제)"]

    D --> E{결제 결과}
    E -->|실패| F["failUrl 리다이렉트\n(?code=xxx&message=xxx)"]
    F --> G["POST /order/payment/fail\n{ orderId, code, message }"]
    G --> H["payment_status = FAILED\nfailure_reason 저장"]
    H --> I["결제 실패 안내 페이지"]

    E -->|성공| J["successUrl 리다이렉트\n(?paymentKey=xxx&orderId=xxx&amount=xxx)"]
    J --> K["POST /order/payment/confirm\n{ paymentKey, orderId, amount }"]
    K --> L["PaymentService:\namount == order.finalPrice 검증"]
    L -->|불일치| M["400 Bad Request\n금액 위변조 감지"]
    L -->|일치| N["TossPaymentGateway.confirm()\nToss API: POST /v1/payments/confirm"]
    N --> O{Toss API 응답}
    O -->|실패| P["payment_status = FAILED\nfailure_reason 저장"]
    O -->|성공| Q["payment_key 저장\npayment_status = PAID\norder_status = PAYED\nDelivery 생성(READY)"]
    Q --> R["200 OK\n주문 완료 페이지 이동"]
```

---

### 2-4-K. 카카오페이 결제 흐름

```mermaid
flowchart TD
    A([인증된 회원]) --> B["결제 수단 선택: 카카오페이\n(Vue.js 결제 페이지)"]

    B --> C["POST /order/payment/ready\n{ orderId, provider: KAKAO }"]
    C --> D["KakaoPaymentGateway.ready()\nKakao API: POST /v1/payment/ready"]
    D --> E["payment 레코드 생성\npg_tid = tid 저장\npayment_status = READY"]
    E --> F["{ tid, redirectPcUrl, redirectMobileUrl } 반환"]

    F --> G{환경 감지}
    G -->|PC| H["next_redirect_pc_url로 이동"]
    G -->|모바일| I["next_redirect_mobile_url로 이동"]

    H --> J["카카오페이 결제창\n(카카오 계정 인증 포함)"]
    I --> J

    J --> K{결제 결과}
    K -->|취소| L["cancel_url → payment_status = FAILED"]
    K -->|실패| M["fail_url → failure_reason 저장"]
    K -->|성공| N["approval_url 리다이렉트\n(?pg_token=xxx)"]

    N --> O["POST /order/payment/approve\n{ orderId, tid, pgToken }"]
    O --> P["PaymentService:\nDB의 pg_tid와 요청 tid 일치 확인"]
    P --> Q["KakaoPaymentGateway.approve()\nKakao API: POST /v1/payment/approve"]
    Q --> R{Kakao API 응답}
    R -->|실패| S["payment_status = FAILED"]
    R -->|성공| T["payment_status = PAID\norder_status = PAYED\nDelivery 생성(READY)"]
    T --> U["200 OK\n주문 완료 페이지 이동"]
```

---

### 2-4-N. 네이버페이 결제 흐름

```mermaid
flowchart TD
    A([인증된 회원]) --> B["결제 수단 선택: 네이버페이\n(Vue.js 결제 페이지)"]

    B --> C["POST /order/payment/ready\n{ orderId, provider: NAVER }"]
    C --> D["NaverPaymentGateway.ready()\nNaver Pay API 준비 요청"]
    D --> E["payment 레코드 생성\npg_tid = paymentId 저장\npayment_status = READY"]
    E --> F["{ paymentId, redirectUrl } 반환"]

    F --> G["Naver Pay JS SDK로\n결제창 오픈\n(네이버 계정 로그인 포함)"]

    G --> H{결제 결과}
    H -->|취소| I["cancelUrl → payment_status = FAILED"]
    H -->|성공| J["returnUrl 리다이렉트\n(?resultCode=Success&paymentId=xxx)"]

    J --> K["POST /order/payment/approve\n{ orderId, paymentId, resultCode }"]
    K --> L{resultCode 확인}
    L -->|Success 아님| M["400 Bad Request\nfailure_reason 저장"]
    L -->|Success| N["NaverPaymentGateway.approve()\nNaver Pay API 승인 요청"]
    N --> O{Naver API 응답}
    O -->|실패| P["payment_status = FAILED"]
    O -->|성공| Q["payment_status = PAID\norder_status = PAYED\nDelivery 생성(READY)"]
    Q --> R["200 OK\n주문 완료 페이지 이동"]
```

---

### 2-5. 주문 취소 ~ 환불 흐름

```mermaid
flowchart TD
    A([인증된 회원]) --> B[OrderController.cancelOrder\nPATCH /mypage/order/{id}/cancel]
    B --> C[OrderService.cancelOrder\norderId + memberId]

    C --> D[OrderService.validateOwnership\n본인 주문 여부 확인]
    D -->|타인 주문| E[403 Forbidden 반환]
    D -->|본인 주문| F{OrderStatus 확인}

    F -->|APPROVAL / 배송 이후| G[400 취소 불가 반환]
    F -->|PENDING / PAYED| H[OrderRepository 조회]

    H --> I[PaymentService.cancelPayment\nPG사 환불 요청]
    I --> J{환불 결과}
    J -->|실패| K[500 환불 실패 반환]
    J -->|성공| L[PaymentStatus = CANCELLED 업데이트]
    L --> M[(payment 테이블 UPDATE\ncancel_reason, cancelled_at)]
    M --> N[OrderStatus = CANCELED 업데이트]
    N --> O[(order 테이블 UPDATE\ncancelled_at = NOW)]
    O --> P[200 OK 취소 완료 반환]
```

---

### 2-5-PG. PG사별 결제 취소 / 환불 흐름

```mermaid
flowchart TD
    A([취소 요청 — 회원 또는 관리자]) --> B["POST /mypage/order/{id}/cancel\nor PATCH /admin/order/{id}/cancel"]
    B --> C["PaymentService.cancelPayment\norderId + cancelReason"]

    C --> D["OrderService.validateOwnership\n본인 주문 또는 관리자 확인"]
    D -->|권한 없음| E["403 Forbidden"]
    D -->|권한 있음| F{OrderStatus 확인}
    F -->|APPROVAL 이후| G["400 취소 불가"]
    F -->|PENDING / PAYED| H["payment.provider 확인"]

    H -->|TOSS| I["TossPaymentGateway.cancel()\nToss API: POST /v1/payments/{paymentKey}/cancel\n{ cancelReason }"]
    H -->|KAKAO| J["KakaoPaymentGateway.cancel()\nKakao API: POST /v1/payment/cancel\n{ cid, tid, cancelAmount }"]
    H -->|NAVER| K["NaverPaymentGateway.cancel()\nNaver Pay API 취소 요청\n{ paymentId, cancelAmount }"]

    I --> L{PG 취소 결과}
    J --> L
    K --> L

    L -->|실패| M["500 Internal Server Error\n상태 유지 (재시도 유도)"]
    L -->|성공| N["payment_status = CANCELLED\ncancelled_at = now()\ncancel_reason 저장"]
    N --> O["order_status = CANCELED\ncancelled_at = now()"]
    O --> P["200 OK 취소 완료"]
```

---

### 2-6. 배송 상태 변경 흐름 (관리자)

```mermaid
flowchart TD
    A([관리자]) --> B{배송 요청 유형}

    B -->|GET /admin/delivery| C[AdminDeliveryController.getDeliveryList]
    B -->|POST /admin/delivery/{id}/tracking| D[AdminDeliveryController.registerTracking]
    B -->|PATCH /admin/delivery/{id}/status| E[AdminDeliveryController.updateDeliveryStatus]

    C --> F[DeliveryService.getDeliveryList]
    F --> G[DeliveryRepository.findAllByStatus\n상태 필터 + 페이징]
    G --> H[(delivery 테이블 SELECT)]
    H --> I[배송 목록 반환]

    D --> J[DeliveryService.registerTracking\nTrackingRequest]
    J --> K[DeliveryRepository.save\ncourier + trackingNumber 업데이트]
    K --> L[(delivery 테이블 UPDATE\ndelivery_status = SHIPPED)]
    L --> M[200 OK 운송장 등록 완료]

    E --> N[DeliveryService.updateDeliveryStatus\nDeliveryStatus]
    N --> O{상태 유효성 검사}
    O -->|역방향 상태 변경| P[400 잘못된 상태 반환]
    O -->|유효| Q{변경 상태}
    Q -->|IN_TRANSIT| R[(delivery_status = IN_TRANSIT UPDATE)]
    Q -->|DELIVERED| S[(delivery_status = DELIVERED\ndelivered_at = NOW UPDATE)]
    Q -->|FAILED| T[(delivery_status = FAILED UPDATE)]
    R --> U[200 OK 상태 변경 완료]
    S --> U
    T --> U
```

---

### 2-7. 리뷰 작성 흐름

```mermaid
flowchart TD
    A([인증된 회원]) --> B[ReviewController.writeReview\nPOST /review]
    B --> C[ReviewService.writeReview\nReviewRequest + memberId]

    C --> D{구매 확정 여부 확인\nOrderItemRepository.findDeliveredItemByMemberAndProduct}
    D -->|구매 이력 없음| E[403 리뷰 작성 권한 없음]
    D -->|구매 확정 이력 있음| F{리뷰 중복 확인\nReviewRepository.existsByMemberIdAndProductId}
    F -->|이미 작성| G[409 중복 리뷰]
    F -->|미작성| H[Review 엔티티 생성\nrating + content]
    H --> I[ReviewRepository.save]
    I --> J[(review 테이블 INSERT)]

    J --> K{이미지 첨부 여부}
    K -->|이미지 있음| L[ReviewImageRepository.saveAll]
    L --> M[(review_image 테이블 INSERT)]
    K -->|없음| N

    M --> N[OrderItemRepository.updateIsReviewed\nis_reviewed = true]
    N --> O[(order_item 테이블 UPDATE)]
    O --> P[201 Created 리뷰 작성 완료]

    A --> Q[ReviewController.addReaction\nPOST /review/{id}/reaction]
    Q --> R[ReviewService.addReaction\nreviewId + reactionType + memberId]
    R --> S{기존 반응 확인\nReviewReactionRepository.findByReviewIdAndMemberId}
    S -->|있음| T[reactionType 토글 업데이트]
    S -->|없음| U[ReviewReactionRepository.save]
    T --> V[(review_reaction 테이블 UPDATE)]
    U --> W[(review_reaction 테이블 INSERT)]
    V --> X[200 OK]
    W --> X
```

---

### 2-8. QnA 문의 ~ 답변 흐름

```mermaid
flowchart TD
    A([인증된 회원]) --> B[QnaController.writeQuestion\nPOST /qna]
    B --> C[QnaService.writeQuestion\nQnaRequest + memberId]
    C --> D[QnaRepository.save\ndepth=0, status=WAITING]
    D --> E[(qna 테이블 INSERT)]
    E --> F[201 Created 문의 등록 완료]

    G([관리자]) --> H[AdminQnaController.getQnaList\nGET /admin/qna?status=WAITING]
    H --> I[QnaService 조회\nQnaRepository.findByStatus]
    I --> J[(qna 테이블 SELECT)]
    J --> K[대기 중 문의 목록 반환]

    G --> L[AdminQnaController.answerQna\nPOST /admin/qna/{id}/answer]
    L --> M[QnaService.answerQuestion\nAnswerRequest + adminId]
    M --> N[답변 Qna 생성\ndepth=1, status=COMPLETE]
    N --> O[QnaRepository.save\n부모 QnaId 연결]
    O --> P[(qna 테이블 INSERT\nparent_id = 원문의 id)]
    P --> Q[원문의 status = COMPLETE 업데이트]
    Q --> R[(qna 테이블 UPDATE\nstatus=COMPLETE, answered_at=NOW)]
    R --> S[201 Created 답변 등록 완료]
```

---

### 2-9. 관리자 대시보드 조회 흐름

```mermaid
flowchart TD
    A([관리자 로그인]) --> B{MemberRole 확인}
    B -->|USER| C[403 Forbidden]
    B -->|MANAGER / ADMIN| D[AdminController.getDashboard\nGET /admin/dashboard]

    D --> E[AdminService.getDashboard]

    E --> F[MemberRepository\n전체 회원 수 집계]
    E --> G[OrderRepository\n주문 상태별 건수 집계]
    E --> H[ProductRepository\n재고 부족 상품 조회]
    E --> I[QnaRepository\n미답변 문의 건수 조회]

    F --> J[(member 테이블 COUNT)]
    G --> K[(order 테이블 GROUP BY order_status)]
    H --> L[(product 테이블 WHERE stock < 임계값)]
    I --> M[(qna 테이블 WHERE status=WAITING COUNT)]

    J --> N[DashboardResponse 조합]
    K --> N
    L --> N
    M --> N
    N --> O[200 OK 대시보드 데이터 반환]
```

---

### 2-10. JWT 인증 미들웨어 흐름

```mermaid
flowchart TD
    A([모든 API 요청]) --> B{공개 API 여부\n/signup /signin 등}
    B -->|공개 API| C[SecurityFilter 통과\n인증 불필요]
    C --> D[Controller 호출]

    B -->|보호된 API| E{Authorization Header 존재 여부}
    E -->|없음| F[401 Unauthorized 반환]
    E -->|있음| G[JwtAuthFilter 실행]

    G --> H[JwtUtil.validateToken\n서명 / 만료 검증]
    H -->|유효하지 않음| I[401 Invalid Token 반환]
    H -->|유효| J[JwtUtil.getMemberId\n토큰에서 memberId 추출]

    J --> K[MemberRepository.findById\n회원 정보 로드]
    K --> L{MemberStatus 확인}
    L -->|DELETE / DEACTIVATE| M[403 접근 불가]
    L -->|ACTIVATE| N{MemberRole 확인}
    N -->|관리자 전용 API + USER 역할| O[403 Forbidden]
    N -->|권한 일치| P[SecurityContextHolder\n인증 정보 저장]
    P --> Q[Controller 호출\n@AuthenticationPrincipal Member]
```

---

## 3. PaymentGateway 인터페이스 구조

```mermaid
classDiagram
    direction LR

    class PaymentGateway {
        <<interface>>
        +ready(PaymentReadyRequest) PaymentReadyResponse
        +confirm(PaymentConfirmRequest) PaymentConfirmResponse
        +cancel(PaymentCancelRequest) void
    }

    class TossPaymentGateway {
        -String secretKey
        -String clientKey
        +ready(PaymentReadyRequest) PaymentReadyResponse
        +confirm(PaymentConfirmRequest) PaymentConfirmResponse
        +cancel(PaymentCancelRequest) void
        -callTossApi(String endpoint, Object body) ResponseEntity
    }

    class KakaoPaymentGateway {
        -String adminKey
        -String cid
        +ready(PaymentReadyRequest) PaymentReadyResponse
        +confirm(PaymentConfirmRequest) PaymentConfirmResponse
        +cancel(PaymentCancelRequest) void
        -callKakaoApi(String endpoint, Object body) ResponseEntity
    }

    class NaverPaymentGateway {
        -String clientId
        -String clientSecret
        +ready(PaymentReadyRequest) PaymentReadyResponse
        +confirm(PaymentConfirmRequest) PaymentConfirmResponse
        +cancel(PaymentCancelRequest) void
        -callNaverApi(String endpoint, Object body) ResponseEntity
    }

    class PaymentService {
        -Map~String, PaymentGateway~ gatewayMap
        +requestReady(PaymentReadyRequest, Long memberId) PaymentReadyResponse
        +confirmPayment(PaymentConfirmRequest, Long memberId) void
        +cancelPayment(Long orderId, String cancelReason) void
        -validateAmount(Long orderId, Integer amount) void
        -resolveGateway(String provider) PaymentGateway
    }

    PaymentGateway <|.. TossPaymentGateway : implements
    PaymentGateway <|.. KakaoPaymentGateway : implements
    PaymentGateway <|.. NaverPaymentGateway : implements
    PaymentService --> PaymentGateway : routes by provider
```

---

## 4. DB 테이블 연관 관계 흐름

```mermaid
flowchart LR
    subgraph 회원
        M[("member\nid / email / password\nname / phone / address\nmember_role / member_status\ndeleted_at")]
    end

    subgraph 상품
        CT[("category_top\nid / name / sort_order\ncategory_top_status")]
        CS[("category_sub\nid / category_top_id\nname / sort_order")]
        P[("product\nid / category_top_id\ncategory_sub_id / title\nauthor / price / stock\nproduct_status / deleted_at")]
        PI[("product_image\nid / product_id\nimage_url / type\nsort_order")]
    end

    subgraph 장바구니
        CA[("cart\nid / member_id")]
        CI[("cart_item\nid / cart_id / product_id\nquantity / is_checked")]
    end

    subgraph 주문_결제_배송
        O[("order\nid / member_id / number\norder_status / total_price\nfinal_price / receiver_name\ndelivery_address\nordered_at / cancelled_at")]
        OI[("order_item\nid / order_id / product_id\nproduct_title / sale_price\nquantity / is_reviewed")]
        PAY[("payment\nid / order_id / method\npayment_status / amount\npg_tid / paid_at\ncancel_reason\n+provider\n+payment_key\n+installment_months\n+failure_reason")]
        DEL[("delivery\nid / order_id / courier\ntracking_number\ndelivery_status\nshipped_at / delivered_at")]
    end

    subgraph 커뮤니티
        N[("notice\nid / member_id\ntitle / content\nis_fixed / view_count\ndeleted_at")]
        Q[("qna\nid / member_id / parent_id\ndepth / category\ntitle / content\nqna_status / is_secret\nanswered_at / deleted_at")]
        R[("review\nid / member_id / product_id\nrating / content\nhits / deleted_at")]
        RI[("review_image\nid / review_id\nimage_url")]
        RR[("review_reaction\nid / review_id / member_id\nreaction_type")]
    end

    M -->|"1:1"| CA
    M -->|"1:N"| O
    M -->|"1:N"| N
    M -->|"1:N"| Q
    M -->|"1:N"| R
    M -->|"1:N"| RR

    CT -->|"1:N"| CS
    CT -->|"1:N"| P
    CS -->|"1:N"| P
    P -->|"1:N"| PI
    P -->|"1:N"| CI
    P -->|"1:N"| OI
    P -->|"1:N"| R

    CA -->|"1:N"| CI
    O -->|"1:N"| OI
    O -->|"1:1"| PAY
    O -->|"1:1"| DEL

    Q -->|"0..1:N 자기참조"| Q
    R -->|"1:N"| RI
    R -->|"1:N"| RR
```

---

## 5. OrderStatus / DeliveryStatus / PaymentStatus 상태 전이 흐름

```mermaid
flowchart LR
    subgraph ORDER_STATUS["📦 OrderStatus 전이"]
        OS1([PENDING\n결제 대기]) -->|결제 완료| OS2([PAYED\n결제 완료])
        OS2 -->|관리자 배송 승인\n구매 확정| OS3([APPROVAL\n구매 확정])
        OS1 -->|결제 실패 / 취소| OS4([CANCELED\n취소 완료])
        OS2 -->|회원 취소 요청| OS4
    end

    subgraph DELIVERY_STATUS["🚚 DeliveryStatus 전이"]
        DS1([READY\n배송 준비]) -->|운송장 등록| DS2([SHIPPED\n발송 완료])
        DS2 -->|배송 중 업데이트| DS3([IN_TRANSIT\n배송 중])
        DS3 -->|배송 완료| DS4([DELIVERED\n배송 완료])
        DS3 -->|배송 실패| DS5([FAILED\n배송 실패])
        DS2 -->|배송 실패| DS5
    end

    subgraph PAYMENT_STATUS["💳 PaymentStatus 전이"]
        PS1([READY\n결제 준비]) -->|PG 승인| PS2([PAID\n결제 완료])
        PS1 -->|PG 실패| PS4([FAILED\n결제 실패])
        PS2 -->|환불 처리\nToss/Kakao/Naver 취소 API| PS3([CANCELLED\n결제 취소])
    end
```

---

## 6. 환경변수 구성 (서버)

> 모든 PG 시크릿 키는 서버 환경변수로만 관리하며 코드·클라이언트에 노출 금지

```
# 토스페이먼츠
TOSS_SECRET_KEY=test_sk_xxxxxxxxxxxxxxxxxxxxxx
TOSS_CLIENT_KEY=test_ck_xxxxxxxxxxxxxxxxxxxxxx

# 카카오페이
KAKAO_ADMIN_KEY=KakaoAK_xxxxxxxxxxxxxxxxxxxxxx
KAKAO_CID=TC0ONETIME

# 네이버페이
NAVER_PAY_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxx
NAVER_PAY_CLIENT_SECRET=xxxxxxxxxxxxxxxxxxxxxx
NAVER_PAY_CHAIN_ID=xxxxxxxxxxxxxxxxxxxxxx
```
